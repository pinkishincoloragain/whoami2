import styled from "styled-components";

const H1 = styled.h1({
  fontSize: "1.4rem",
  fontWeight: "bold",
});

const H2 = styled.h2({
  fontSize: "1.2rem",
  fontWeight: "bold",
});

const H3 = styled.h3({
  fontSize: "1rem",
  fontWeight: "bold",
});

export { H1, H2, H3 };
